name: LeanEval low-degree radical probe (archived)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Archived exact workflow: competitions/lean_eval/archive/workflows/lean-eval-low-degree-probe.yml'
