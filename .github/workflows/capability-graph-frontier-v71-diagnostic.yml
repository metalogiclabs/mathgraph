name: V71 Capability Graph Frontier Diagnostic

on:
  push:
    branches: [capability-graph-frontier-v71-diagnostic]
    paths:
      - 'experiments/capability_graph_frontier_transfer_v71/**'
      - '.github/workflows/capability-graph-frontier-v71-diagnostic.yml'
  workflow_dispatch:

jobs:
  diagnose-capability-graph-frontier:
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - name: Static check
        run: |
          python3 -m py_compile experiments/capability_graph_frontier_transfer_v71/run_calibration.py
          python3 -m py_compile experiments/capability_graph_frontier_transfer_v71/run_diagnostic.py
      - name: Install inherited frozen backend
        run: python3 -m pip install --disable-pip-version-check cvc5==1.3.1
      - name: Fetch opened unlabeled law universe
        run: |
          set -euo pipefail
          mkdir -p external-v71-diag
          curl -fsSL --retry 3 https://raw.githubusercontent.com/heathsanchez/equational-theories-lean-stage2/374eb40ba5389915deb174fbc12cc92346214dd1/examples/problems/eq_size5.txt -o external-v71-diag/eq_size5.txt
          test "$(git hash-object external-v71-diag/eq_size5.txt)" = "0deca9c8860fb60f2ae161ab7e3f458ec3b178ca"
          echo "V71_DIAGNOSTIC_ONLY=NO_CLAIM"
      - name: Run fast V71 diagnostic
        run: |
          PYTHONPATH=. python3 experiments/capability_graph_frontier_transfer_v71/run_diagnostic.py \
            --laws external-v71-diag/eq_size5.txt \
            --out external-v71-diag/results
      - name: Preserve diagnostic evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: v71-capability-graph-frontier-diagnostic
          path: external-v71-diag/results/
