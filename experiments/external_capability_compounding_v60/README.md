# V60 — term-defined operation compilation

V59 showed that local table geometry does not transfer. V60 changes the
representation: a retained algebra is a substrate for executable term
constructors.

The calibration grammar contains every binary term in x,y using at most two
parent-operation nodes (22 total). Constructor terms are selected only on
V58-opened rows 2628:2756, then evaluated without retuning on the later
V59-opened rows 2756:2820. Fresh evidence remains untouched at row 2820+.

A derived operation is terminal only when MathGraph exhaustively verifies the
source identity and a target failure. No published proof or verdict is used.
